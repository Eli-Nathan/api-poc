'use strict';

/**
 * directions-killswitch service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::directions-killswitch.directions-killswitch');
