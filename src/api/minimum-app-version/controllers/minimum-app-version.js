'use strict';

/**
 *  minimum-app-version controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::minimum-app-version.minimum-app-version');
